fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pyyaml==6.0.2
pytest==8.3.4
httpx==0.28.1

# Ingestion. Declared in CLAUDE.md's fixed stack; the inherited repo never
# installed them because it loaded no real data.
pandas==2.2.3
numpy==2.1.3
# Agency canonicalisation. Folds nothing on the current corpus (the portal
# publishes one IDA string per district office) but every raw string still
# passes through it and lands in agency_name_variants for review.
rapidfuzz==3.10.1
# The ML tier (F7). Declared in the fixed stack; installed in the working
# environment but never pinned here, so a fresh checkout could not build the
# package that uses them.
scikit-learn==1.5.2
networkx==3.4.2
# Authentication (Phase 6). Declared in CLAUDE.md's fixed stack and in
# PROJECT-BRIEF.md's honest-scoping note since Stage 0, but never pinned here -
# the same defect the ML pins above record. bcrypt is pinned to 4.0.1 because
# passlib 1.7.4 reads `bcrypt.__about__`, which 4.1 removed: the pairing works
# and emits a warning on every hash otherwise.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
